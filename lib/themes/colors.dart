import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';
class AppColor {
  // Primary Color
  static const Color primary = Color.fromRGBO(255,138,128,1);
  static const Color primary_light = Color.fromRGBO(255,188,175,1);
  static const Color primary_dark = Color.fromRGBO(200,90,84,1);

  // Secondary Color
  static const Color secondary = Color.fromRGBO(255,204,188,1);
  static const Color secondary_light = Color.fromRGBO(255,238,222,1);
  static const Color secondary_extraLight = Color.fromRGBO(255,252,251,1);
  static const Color secondary_dark = Color.fromRGBO(203,155,140,1);

  // Dark Accent where contrast is required
  static const Color accent = Color.fromRGBO(1,1,1,1);
  static const Color accent_light = Color.fromRGBO(1,1,1,1);
  static const Color accent_dark = Color.fromRGBO(1,1,1,1);

  // Primaries
  static const Color red = Color.fromRGBO(255, 70, 70, 1);
  static const Color green = Color.fromRGBO(221,251,224,1.0);
  static const Color yellow = Color.fromRGBO(255,189,92,1);
  static const Color blue = Color.fromRGBO(1,1,1,1);

  // Monochrome Colors
  static const Color black = Color.fromRGBO(33,33,33,1);
  static const Color white = Color.fromRGBO(255,255,255,1);
  static const Color gray_light = Color.fromRGBO(189,189,189,1);
  static const Color gray_dark = Color.fromRGBO(117,117,117,1);
  static const Color gray_transparent = Color.fromRGBO(189,189,189,.15);

  // Font Colors
  static const Color color_font_dark = Color.fromRGBO(78, 93, 115,1);
  static const Color color_font_light = Color.fromRGBO(255,238,222,1);
}